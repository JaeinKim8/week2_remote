#include <iostream>
using namespace std;

int main(){

    int a,b,c;
    cin>>a>>b>>c;

    if ((a != 1 && a != 4 && a != 7) && (b != 1 && b != 4 && b != 7) && (c != 1 && c != 4 && c != 7)){
        cout<<a<<b<<c;
    }
}
